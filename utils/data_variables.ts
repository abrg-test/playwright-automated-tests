// Global variables with fixed data values used for the tests
import { hiddenEmail, hiddenPassword } from '../utils/hidden_data';

export const userEmail = hiddenEmail;
export const userPassword = hiddenPassword;
